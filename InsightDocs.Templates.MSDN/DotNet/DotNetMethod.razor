<html>
    <head>
        <link rel="stylesheet" href="../../msdn.css"/>
    </head>
    <body>
        <main id="main" role="main" class="layout-body-main " data-bi-name="content" lang="en-us" dir="ltr">
            <div data-main-column="" class="padding-sm padding-top-none padding-top-sm-tablet">
				<div>
                    <div class="content">
                        <div data-chunk-ids="0">
                            <h1>
                                @if (Item!.DeclaringType != null)
                                {
                                    @(Item.DeclaringType.DisplayName + "." + Item.DisplayName + " Method")
                                }

                                else
                                {
                                    @(Item.DisplayName + " Method")
                                }
                            </h1>

                            <h2>Definition</h2>

                            <div class="metadata">
                                @if (Item.DeclaringType?.Namespace != null)
                                {
                                    <dl class="attributeList">
                                        <dt>Namespace:</dt>
                                        <dd>@RazorTemplate.GetLink(Item.DeclaringType.Namespace, Item.DeclaringType.Namespace.FullName, ServiceProvider!)</dd>
                                    </dl>
                                }

                                @if (!String.IsNullOrEmpty(Item.DeclaringType?.Assembly?.Name))
                                {
                                    <dl class="attributeList">
                                        <dt>Assembly:</dt>
                                        <dd>@Item.DeclaringType.Assembly.Name</dd>
                                    </dl>
                                }
                            </div>

                            <div>
                                @if (Item.Description != null)
                                {
                                    <div class="summaryHolder">@RazorTemplate.Raw(Item.Description.ToHtml())</div>
                                }

                                <!-- TODO: code -->

                                @if (Item.Parameters != null)
                                {
                                    <h4 class="propertyInfoTitle stack heading-anchor" id="parameters">Parameters</h4>

                                    @for (int i = 0; i < Item.Parameters.Count; i++)
                                    {
                                        <div class="@(i == 0 ? "propertyInfo" : "propertyInfo stack")">
                                            <dl class="parameterList">
                                                <dt>@Item.Parameters[i].Name</dt>
                                                <dd>@RazorTemplate.GetLink(Item.Parameters[i].Type, Item.Parameters[i].Type.DisplayName, ServiceProvider!)</dd>
                                            </dl>

                                            @if (Item.Parameters[i].Description != null)
                                            {
                                                <p>@RazorTemplate.Raw(Item.Parameters[i].Description!.ToHtml())</p>
                                            }
                                        </div>
                                    }
                                }

                                <h4 class="propertyInfoTitle stack">Returns</h4>

                                <div class="propertyInfo">@RazorTemplate.GetLink(Item.ReturnType, Item.ReturnType.DisplayName, ServiceProvider!)</div>

                                @if (Item.ReturnsDescription != null)
                                {
                                    @RazorTemplate.Raw(Item.ReturnsDescription.ToHtml(), "p")
                                }

                                @if (Item.Remarks != null)
                                {
                                    <div class="heading-wrapper" data-heading-level="h2">
                                        <h2 id="remarks" class="heading-anchor">Remarks</h2>
                                    </div>

                                    @RazorTemplate.Raw(Item.Remarks.ToHtml(), "p")
                                }
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </body>
</html>

@code {
    [Parameter]
    public Model.DotNet.DotNetMethod? Item 
    { 
        get; 
        set; 
    }

    [Parameter]
    public IServiceProvider? ServiceProvider
    { 
        get; 
        set; 
    }
}