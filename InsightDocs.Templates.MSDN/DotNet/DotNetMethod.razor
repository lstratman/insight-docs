<html>
    <head>
        <link rel="stylesheet" href="../../msdn.css"/>
    </head>
    <body>
        <main id="main" role="main" class="layout-body-main " data-bi-name="content" lang="en-us" dir="ltr">
            <div data-main-column="" class="padding-sm padding-top-none padding-top-sm-tablet">
				<div>
                    <div class="content">
                        <div data-chunk-ids="0">
                            <h1>
                                @if (Item!.DeclaringType != null)
                                {
                                    @(Item.DeclaringType.DisplayName + "." + (Item.Overloads.Count == 1 ? Item.Overloads[0].DisplayName : Item.Name) + " Method")
                                }

                                else
                                {
                                    @((Item.Overloads.Count == 1 ? Item.Overloads[0].DisplayName : Item.Name) + " Method")
                                }
                            </h1>

                            <h2>Definition</h2>

                            <div class="metadata">
                                @if (Item.DeclaringType?.Namespace != null)
                                {
                                    <dl class="attributeList">
                                        <dt>Namespace:</dt>
                                        <dd>@RazorTemplate.GetLink(Item.DeclaringType.Namespace, Item.DeclaringType.Namespace.FullName, ServiceProvider!)</dd>
                                    </dl>
                                }

                                @if (!String.IsNullOrEmpty(Item.DeclaringType?.Assembly?.Name))
                                {
                                    <dl class="attributeList">
                                        <dt>Assembly:</dt>
                                        <dd>@Item.DeclaringType.Assembly.Name</dd>
                                    </dl>
                                }
                            </div>

                            @if (Item.Overloads.Count(o => o.DeclaringType != null && Item.DeclaringType != null && o.DeclaringType.Type == Item.DeclaringType.Type) > 1)
                            {
                                <h2 id="overloads">Overloads</h2>
                            }

                            @foreach (Model.DotNet.DotNetMethodOverload overload in Item.Overloads.Where(o => o.DeclaringType != null && Item.DeclaringType != null && o.DeclaringType.Type == Item.DeclaringType.Type))
                            {
                                <div>
                                    @if (Item.Overloads.Count(o => o.DeclaringType != null && Item.DeclaringType != null && o.DeclaringType.Type == Item.DeclaringType.Type) > 1)
                                    {
                                        <div class="heading-wrapper" data-heading-level="h2">
                                            <h2 class="memberNameHolder heading-anchor">@overload.DisplayName</h2>
                                        </div>

                                        @RazorTemplate.Raw("<div class=\"memberInfo\">")
                                    }

                                    @if (overload.Description != null)
                                    {
                                        <div class="summaryHolder">@RazorTemplate.Raw(overload.Description.ToHtml())</div>
                                    }

                                    <!-- TODO: code -->

                                    @if (overload.Parameters != null)
                                    {
                                        <h4 class="propertyInfoTitle stack heading-anchor" id="parameters">Parameters</h4>

                                        @for (int i = 0; i < overload.Parameters.Count; i++)
                                        {
                                            <div class="@(i == 0 ? "propertyInfo" : "propertyInfo stack")">
                                                <dl class="parameterList">
                                                    <dt>@overload.Parameters[i].Name</dt>
                                                    <dd>@RazorTemplate.GetLink(overload.Parameters[i].Type, overload.Parameters[i].Type.DisplayName, ServiceProvider!)</dd>
                                                </dl>

                                                @if (overload.Parameters[i].Description != null)
                                                {
                                                    <p>@RazorTemplate.Raw(overload.Parameters[i].Description!.ToHtml())</p>
                                                }
                                            </div>
                                        }
                                    }

                                    <h4 class="propertyInfoTitle stack">Returns</h4>

                                    <div class="propertyInfo">@RazorTemplate.GetLink(overload.ReturnType, overload.ReturnType.DisplayName, ServiceProvider!)</div>

                                    @if (overload.ReturnsDescription != null)
                                    {
                                        @RazorTemplate.Raw(overload.ReturnsDescription.ToHtml(), "p")
                                    }

                                    @if (overload.Remarks != null)
                                    {
                                        <div class="heading-wrapper" data-heading-level="h2">
                                            <h2 id="remarks" class="heading-anchor">Remarks</h2>
                                        </div>

                                        @RazorTemplate.Raw(overload.Remarks.ToHtml(), "p")
                                    }

                                    @if (Item.Overloads.Count(o => o.DeclaringType != null && Item.DeclaringType != null && o.DeclaringType.Type == Item.DeclaringType.Type) > 1)
                                    {
                                        @RazorTemplate.Raw("</div>")
                                    }
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </body>
</html>

@code {
    [Parameter]
    public Model.DotNet.DotNetMethod? Item 
    { 
        get; 
        set; 
    }

    [Parameter]
    public IServiceProvider? ServiceProvider
    { 
        get; 
        set; 
    }
}